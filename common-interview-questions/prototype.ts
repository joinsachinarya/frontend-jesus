// In javascript every object has a hidden key named [[Prototype]] which can be either null or references another object,
// that object is called 'prototype'. 
// We can access this prototype using obj.__proto__ or Object.getPrototypeOf(obj).
