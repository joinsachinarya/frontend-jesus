const arr = [1, 2, 3]
console.log(arr.map((it)=>it*2)) // map  creates and returns a new array by 
// performing some specific operation on every element of the array

console.log(arr.forEach((item)=>item*4)) // foreach also perform some specific operation on every element 
// of array but it does not create  a new array and also does not return any thing
